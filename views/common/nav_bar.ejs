<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1"
        aria-expanded="false">
      <span class="sr-only">Toggle navigation</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
      <a class="navbar-brand" href="/">DBProject</a>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <% if (typeof user !== "undefined") { %>

          <% if (user.type === 'M') { %>
            <% if (location === 'manager-board') { %>
            <li class="active"><a href="/manager-board">Manager Board <span class="sr-only">(current)</span></a></li>
            <% } else { %>
            <li><a href="/manager-board">Manager Board</a></li>
            <% } %>
          <% } %>

          <% if (user.type === 'A') { %>
            <% if (location === 'advisor') { %>
            <li class="active"><a href="/advisor">Advisor <span class="sr-only">(current)</span></a></li>
            <% } else { %>
            <li><a href="/advisor">Advisor</a></li>
            <% } %>
          <% } %>

          <% if (location === 'student-info') { %>
          <li class="active"><a href="/student-info">Student Info <span class="sr-only">(current)</span></a></li>
          <% } else { %>
          <li><a href="/student-info">Student Info</a></li>
          <% } %>

          <% if (location === 'course') { %>
          <li class="active"><a href="/course">Course <span class="sr-only">(current)</span></a></li>
          <% } else { %>
          <li><a href="/course">Course</a></li>
          <% } %>

          <% if (location === 'about') { %>
          <li class="active"><a href="/about">About <span class="sr-only">(current)</span></a></li>
          <% } else { %>
          <li><a href="/about">About</a></li>
          <% } %>

        <% } %>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
            <%= typeof user !== "undefined" ? user.display_name : "Not Logged In" %> <span class="caret"></span>
          </a>
          <ul class="dropdown-menu">
            <li><a href="/logout">Logout</a></li>
          </ul>
        </li>
      </ul>
    </div>
  </div>
</nav>
